# syntax=docker/dockerfile:1
FROM alpine
WORKDIR /opt/app
COPY . .
CMD ["/bin/sh","/opt/app/example.sh"]
# scratch 不存在 /bin/sh 会报错
# CMD "/opt/app/example.sh" = CMD ["/bin/sh","/opt/app/example.sh"]，所以正常执行都通过CMD []